const btns=document.querySelectorAll('button');
btns.forEach((btn)=>{
    btn.addEventListener('click')
    //alert(id);
});

/*function sensor(){
    btn.forEach((button)=>{
        button.addEventListener('click', button.getAttribute('id'))
        alert(id);
    });
}
    
sensor();*/





